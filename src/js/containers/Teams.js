import React from 'react';

const Teams = () => {
    return (
        <div>
            <h1>Teams</h1>
             <p>With observables we can easily manage asynchronous data streams, but what is a stream in a React component?</p>
             <p>With observables we can easily manage asynchronous data streams, but what is a stream in a React component?</p>
             <p>With observables we can easily manage asynchronous data streams, but what is a stream in a React component?</p>
        </div>
    );
};

export default Teams;