import React from 'react';

const Profile = () => {
    return (
        <div>
            <h1>Profile</h1>
             <p> Using observables is clearly a good thing. So why don’t we use it more often? More specifically, why don’t we use it in React apps? </p>
        </div>
    );
};

export default Profile;